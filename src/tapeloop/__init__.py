"""tapeloop — an agent runtime that records every step.

The package is intentionally empty until M1. M0 lives in ``m0/loop.py`` as a
single unabstracted file, so the protocol is visible before anything hides it.
"""

__version__ = "0.0.0"
